#include <stdio.h>

int main(void) {
    printf("%s\t%s\t%s\n", "number", "square", "cube");

    int n = 0;
    printf("%d\t%d\t%d\n", n, n * n, n * n * n);

    n = 1;
    printf("%d\t%d\t%d\n", n, n * n, n * n * n);

    n = 2;
    printf("%d\t%d\t%d\n", n, n * n, n * n * n);

    n = 3;
    printf("%d\t%d\t%d\n", n, n * n, n * n * n);

    n = 4;
    printf("%d\t%d\t%d\n", n, n * n, n * n * n);

    n = 5;
    printf("%d\t%d\t%d\n", n, n * n, n * n * n);

    n = 6;
    printf("%d\t%d\t%d\n", n, n * n, n * n * n);

    n = 7;
    printf("%d\t%d\t%d\n", n, n * n, n * n * n);

    n = 8;
    printf("%d\t%d\t%d\n", n, n * n, n * n * n);

    n = 9;
    printf("%d\t%d\t%d\n", n, n * n, n * n * n);

    n = 10;
    printf("%d\t%d\t%d\n", n, n * n, n * n * n);

    return 0;
}